{
  "name": "beth-cooper",
  "version": "1.0.0",
  "description": "Beth Cooper's landing page",
  "scripts": {
    "build": "rmdir /s /q dist && mkdir dist && copy index.html dist\\index.html && copy styles.css dist\\styles.css && copy script.js dist\\script.js && copy netlify.toml dist\\netlify.toml && xcopy /E /Y assets dist\\assets",
    "start": "serve dist",
    "dev": "serve ."
  },
  "author": "John Wesley Quintero",
  "license": "MIT",
  "dependencies": {
    "serve": "^14.2.4"
  }
}
